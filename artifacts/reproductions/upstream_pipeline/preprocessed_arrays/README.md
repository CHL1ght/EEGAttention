# 上游预处理数组

| 文件 | 含义 |
|---|---|
| `X_train.npy` / `X_test.npy` | 上游 notebook 导出的训练/测试特征数组。 |
| `y_train.npy` / `y_test.npy` | 与上述数组对应的标签数组。 |

这是上游历史随机窗口流程的缓存；数组已缩放但没有保存可复用的 scaler，因此不能安全地用于当前新 EDF 推理。
