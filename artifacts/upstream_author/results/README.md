# 原作者训练结果表

本目录的 15 个 `.xlsx` 是上游项目原始版本已经存在的训练结果表。文件名由网络结构（`googlenet`、`resnet18`、`group`）和特征处理方式（原始、`anova`、`fi`、`lcc`、`pca`）组成。

它们用于来源追溯和历史对照，不是本项目新生成的模型，也没有对应的当前 Scaler/PCA/SVC pipeline；不用于 LOCKED_TEST。
