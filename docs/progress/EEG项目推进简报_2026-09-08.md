# EEG 项目推进简报｜2026-09-08

**当前里程碑**：`00｜冻结旧数据与旧模型` 部分完成。

- `00.1` Legacy 数据封存：已完成。
- `00.2` 预处理/特征/PCA/SVC 参数冻结：等待规范主 Notebook。
- `00.3` 完整 recording/session 隔离：规则已确定，等待主 Notebook 强制执行。
- LOCKED_TEST 数据封存：已完成。
- 首次独立盲测：未执行。

## 一句话结论

数据目录和 LOCKED_TEST 口径已经固定，但传统 baseline 尚未冻结。旧单状态自采数据将作为 Legacy 训练/验证来源，9 月 7 日数据保持为 LOCKED_TEST。当前没有保存好的 Scaler→PCA→SVC Pipeline，需要由规范主 Notebook 重新训练并冻结 `legacy_baseline_v0`。

## 已落实的数据口径

| 项目 | 结果 |
|---|---|
| 新录制标签 | 一个文件只对应一种状态；文件名 + `session_manifest.csv` 是标签真值，不依赖 marker |
| 二分类标签 | `focus` 与 `unfocus` |
| 静息态 | `death` 统一记为 `rest`，只作基线/质量检查 |
| Legacy 训练候选 | 旧 `multiclass_10min` 中 `focus → focus`，`iu + ou → unfocus`，`daze` 排除二分类 |
| 旧混合数据 | `mixed_20min` 的切段顺序记录存在冲突，只封存，不再人工切段训练 |
| 窗口 | 4 秒窗、2 秒步长；只保留完整窗口 |
| 活动时间误差 | 新数据每段首尾默认各丢弃 30 秒；准确起止时间可在清单逐行覆盖 |
| 通道与采样 | 7 个锁定 EDF 均为 300 Hz、26 路记录，通道集合一致 |
| 防泄漏 | 先按完整 EDF/session/被试划分，再切窗；LOCKED_TEST 禁止训练和调参 |

## 产物来源校正

- 原作者早期仓库中已有的 15 份结果表位于 `artifacts/upstream_author/results/`。
- 四个 `.npy`、特征缓存、GoogLeNet/ResNet18 权重及后续结果，与修改后的上游复现流程一同于提交 `68a25c0` 中加入。它们位于 `artifacts/reproductions/upstream_pipeline/`。
- 上述复现产物使用原作者 MAT 数据，但文件本身不是原作者直接附带的。
- GoogLeNet/ResNet18 历史权重不是当前正式模型，不用于 LOCKED_TEST。

## 为什么还不能盲测

旧 Notebook 曾训练 DT、RF、KNN、LR 和 SVC，但传统模型只存在于当时的 Notebook 内存中。仓库及 Git 历史中都没有可直接载入的 `.joblib/.pkl/.pickle` 模型。

旧 `X_train/X_test.npy` 虽然已经过 StandardScaler，但当时的 Scaler 没有保存，因此不能对新 EDF 严格重现同样的 `transform`。

## 当前锁定集

- 共登记 7 个 session、2 名被试。
- 正式二分类 LOCKED_TEST：6 段，按统一窗口可产生 2389 个窗口。
- 静息参考：1 段，124 秒；不计入二分类指标。
- EDF 内容已用 SHA-256 锁定；当前验收为 0 错误。

## 下一步

完成 `00.2/00.3`：编写规范主 Notebook，从 Legacy 单状态数据按完整 EDF/session 划分训练与验证，只在训练集 fit Scaler/PCA/SVC，最终保存完整的 `legacy_baseline_v0`。在此之前不执行 LOCKED_TEST 盲测。

详细口径见 `docs/methodology/legacy_baseline_v0.md`。
