# EEG 项目推进简报｜2026-09-13

记录性质：Legacy baseline 冻结、2026-09-07 `LOCKED_TEST FIRST RUN` 已完成，并完成可读性重构与 2026-09-14 录制前准备；本简报不重新训练或重跑 LOCKED_TEST。

- 当前分支：`better_train`；冻结快照 commit 为 `431f6c63c2e0f1461e67bf5f49b18a68b45b916d`，LOCKED_TEST 使用冻结 pipeline，只做 inference。
- `00.1` Legacy 数据封存：已完成。
- `00.2` 预处理/特征/PCA/SVC 冻结：已完成。入口为 `scripts/legacy_baseline_v0.py`，产物为 `artifacts/legacy_baseline_v0/`。
- `00.3` 完整 recording/session 隔离：已完成。train 17 个 `session_group_id` / 25 个 source，validation 5 个 / 9 个；两类交集均为 0。
- Legacy validation：3,282 个 validation windows，accuracy `0.699878`；confusion matrix（`unfocus, focus`）为 `[[1315, 475], [510, 982]]`。
- 冻结配置：128 Hz、0.5–43 Hz FIR bandpass、4 s/2 s window、Welch 五频段 log+relative power、`StandardScaler → PCA(0.95) → RBF SVC(C=10, balanced)`。
- LOCKED_TEST（2026-09-07）：2,389 windows，accuracy `0.552951`，balanced accuracy `0.599068`；相对 Legacy validation accuracy 差约 `-0.146927`。未使用 LOCKED_TEST fit / 重训 / 调参，正式结果见 `artifacts/locked_test/2026-09-07/REPORT.md`。
- 可读性重构：已完成，结果未改变；已有 REPORT，regression checks 通过。本次不因重构重新训练或重新运行 LOCKED_TEST。
- 2026-09-14 录制准备：protocol、recording plan、recording notes 模板已完成。设备为 DSIStreamer、300 Hz、24 EEG、Pz reference；Trigger 实际全为 0，标签主要依赖文件名 / session metadata，而不是 marker。
- 现场快速测试：`notebooks/lab_quick_test_legacy_model.ipynb` 已完成输入 EDF 路径、按文件名解析标签并复用冻结模型只读推理；本地保留一次成功执行输出。教学注释副本位于 `scripts/annotated/`。
- 下一步：先录明确状态的短 EDF；practice / calibration 段可查看旧模型反馈并调整任务方式；最后单独录制不查看反馈的真正 LOCKED_TEST。现场不做复杂归一化、个体微调或重训。
