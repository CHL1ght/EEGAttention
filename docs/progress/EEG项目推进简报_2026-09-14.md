# EEG 项目推进简报｜2026-09-14

## 本阶段完成

本阶段先诊断上一阶段的 lyc/zyf personal model，再检查原作者 `.mat` 数据并建立 author-only model。全程没有覆盖旧 pooled frozen model、personal model 或 LOCKED_TEST 结果。

## 1. Personal model 诊断

历史训练数据按完整 `session_group_id` 做 Leave-One-Session-Group-Out；同一 EDF/session 的窗口没有跨 train/test。LOCKED_TEST 只读取上一阶段已有 prediction CSV 做统计，fit 次数为 0。

| subject | 历史 session groups | 历史 windows | focus / unfocus windows | LOGO accuracy | LOGO balanced accuracy |
|---|---:|---:|---:|---:|---:|
| lyc | 12 | 6,741 | 3,758 / 2,983 | 71.67% ± 29.00% | 75.39% ± 13.62%（7 个有效 fold） |
| zyf | 7 | 4,483 | 2,096 / 2,387 | 74.58% ± 12.80% | 75.71% ± 13.71%（6 个有效 fold） |

`lyc personal → zyf` 的 LOCKED_TEST accuracy 为 67.94%，但 balanced accuracy 只有 51.80%；预测 focus 比例为 98.57%，真实 focus 比例为 66.83%。因此该高 accuracy 明确来自类别预测偏置，不能解释为跨受试者泛化良好。

历史 held-out 明显高于对应 LOCKED_TEST，当前证据支持 session/domain shift 是重要嫌疑，但不排除采集范式、标签和数据量因素。

## 2. Author `.mat` 检查

- 数据位置：`data/reference/original_mat/`，共 34 个 `.mat`；当前沿用上游 notebook 的有效 recording 选择，使用 `eeg_record3–7`、`10–14`、`17–21`、`24–27`、`31–34`，共 23 个 recording。
- 结构：顶层 `o` struct，信号为 `o.data`，每个文件为约 `N × 25`，采样率 `128 Hz`；上游逻辑取 `o.data[:20*128*60, 3:17]` 的 14 个 EEG 通道。
- 明确通道顺序：`AF3, F7, F3, FC5, T7, P7, O1, O2, P8, T8, FC6, F4, F8, AF4`。
- 标签：每个 recording 的 `[0, 600)` 秒为 focus，`[600, 1200)` 秒为 unfocus。
- 原作者 notebook 使用 `IModPoly → Butterworth 0.2–43 Hz → STFT → 36 频率 bin → 15 帧滑窗`，形成 252 维特征；随后在拼接后的窗口上执行 `train_test_split`。因此同一 recording 的窗口可能跨 train/test，存在 window-level leakage。原 notebook 的 Scaler/PCA/SVC 是 train 侧 fit，但不能消除该 split 泄漏。

本仓库 author-only 模型采用共享的 MNE FIR `0.5–43 Hz`、4 秒窗口/2 秒步长、Welch bandpower 和 7 通道 70 维特征，以便未来与 our-common7 统一比较；这不是对旧 STFT notebook 的逐行复制。

## 3. Author-only 结果

使用 23 个 recording、46 个 label block、13,754 个窗口；5-fold `GroupKFold(session_group_id)` 验证：

- accuracy：`64.12% ± 4.37%`
- balanced accuracy：`64.12% ± 4.37%`
- 最终模型 PCA：23 维，累计解释方差 95.41%

逐 recording MAT 结构见 `artifacts/author_models/author_only/mat_inspection.csv`。

## 4. Common7 阻塞

当前正式 LOCKED_TEST EDF 的 MNE 通道可明确匹配 `F7-Pz`、`F3-Pz`、`O1-Pz`、`O2-Pz`，但缺少 `P7`、`P8`、`AF4`。本阶段没有把 `T5/T6` 猜作 P7/P8，因此：

- author-only → lyc/zyf EDF：N/A；
- our-common7 pooled：blocked，未生成 pipeline；
- our+author mixed：blocked，未生成 pipeline。

## 5. 产物与入口

- Personal diagnosis：`artifacts/subject_model_diagnostics/2026-09-07/REPORT.md`
- Author-only model：`artifacts/author_models/author_only/REPORT.md`
- Unified table：`artifacts/cross_source_comparison/2026-09-07/REPORT.md`
- Personal diagnosis entry：`python scripts/diagnose_subject_models.py`
- Author/common7/mixed training entry：`python scripts/train_cross_source_models.py --model author-only|our-common7|mixed`
- Unified comparison entry：`python scripts/evaluate_cross_source_models.py`

## 6. 数据隔离与 Git 检查

- `zqd` 的 6 条历史 candidate 记录未进入 personal 或 our-data 模型；未知身份不猜测。
- 11 个未被上游 notebook 选择的 author `.mat`（`1,2,8,9,15,16,22,23,28,29,30`）未进入 author-only 训练。
- LOCKED_TEST 未进入任何 scaler/PCA/SVC fit；cross-source 训练只在通道门禁阶段读取 EDF header，不使用测试统计量。
- 既有 `artifacts/legacy_baseline_v0/`、`artifacts/subject_models/` 和 `artifacts/locked_test/` 未被覆盖。
- `git diff --check` 通过；relative Markdown link 检查通过。

## 7. Common6 兼容性核验（外部证据补充前的历史状态）

本轮先按要求检查当前设备、EDF 原始头、DSIStreamer CSV sidecar、仓库采集协议、作者 MAT 字段和作者 notebook。外部权威证据补充前的历史结论是 **BLOCKED**；该判断过程保留如下：

- 当前 EDF 的 43 个已确认 lyc/zyf 历史及 LOCKED_TEST/reference 文件均有 `T5-Pz`、`T6-Pz`，没有 literal `P7`、`P8`；CSV sidecar 的 36 个文件均写 `Reference location: Pz`。
- 项目协议明确记录 DSIStreamer、300 Hz、24 路 EEG、Pz reference；这能支持我们的 EDF 为 Pz reference 的设备声明，但没有提供 `T5/T6 → P7/P8` 的设备 montage/electrode mapping。
- 34 个作者 MAT 的 `o` struct 只有 `id/tag/nS/sampFreq/marker/timestamp/data/trials` 等字段，没有 reference、montage 或 electrode metadata；作者 notebook 明确选择 P7/P8，但没有显式 rereference 操作或作者侧 reference 说明。
- 因此没有建立 `COMMON_6_CHANNELS`，没有训练 `author-common6`、`our-common6`、`mixed-common6`，也没有为 common6 读取 LOCKED_TEST 信号做 prediction-only 对比；旧 pooled、personal、author-only-7ch 和 LOCKED_TEST 产物均未改写。

详细证据和阻塞解锁条件见 [`artifacts/common6_compatibility/2026-09-14/REPORT.md`](../../artifacts/common6_compatibility/2026-09-14/REPORT.md)。可复核入口为 `python scripts/verify_common6_compatibility.py`。

## 8. Common6 权威证据更新与三模型比较

随后收到并核对了两层权威外部证据：ACNS 将旧 10–20 `T5/T6` 对应到 modified 10–10 `P7/P8`，Wearable Sensing DSI-24 规格直接列出 `P7/T5`、`P8/T6`；Wearable Sensing 技术文档确认 DSI-24/DSI-Streamer 使用 Pz hardware reference。因此本阶段将历史 `BLOCKED` 状态更新为 `unblocked_for_common6_training`，原始阻塞报告保存在 `HISTORICAL_*` 快照中。

建立的 common6 顺序为 `F7,F3,P7,O1,O2,P8`：`F7-Pz→F7`、`F3-Pz→F3`、`T5-Pz→P7`、`O1-Pz→O1`、`O2-Pz→O2`、`T6-Pz→P8`；主动舍弃 `AF4`。Our EDF reference 为 `Pz (confirmed)`，author MAT reference 仍为 `unknown`，所以任何跨源结果都标注 exploratory / channel-aligned but reference compatibility uncertain。

### 8.1 训练与验证

| model | recordings / source groups | GroupKFold Accuracy | GroupKFold Balanced Accuracy |
|---|---:|---:|---:|
| author-only-7ch | 23 author recordings | 64.12% ± 4.37% | 64.12% ± 4.37% |
| author-common6 | 23 author recordings | 64.69% ± 4.29% | 64.69% ± 4.29% |
| our-common6 | 19 lyc/zyf recording groups | 69.83% ± 6.98% | 69.51% ± 7.21% |
| mixed-common6 | 42 author + lyc/zyf recording groups | 67.19% ± 3.27% | 67.31% ± 3.26% |

去掉 AF4 后作者内部 GroupKFold balanced accuracy 上升约 0.57 个百分点；这只是作者数据内部的通道 ablation，不代表跨设备增益。

### 8.2 LOCKED_TEST 结果

| Model | lyc Acc | lyc Bal Acc | zyf Acc | zyf Bal Acc |
|---|---:|---:|---:|---:|
| Existing pooled frozen | 65.54% | 64.28% | 46.11% | 57.69% |
| lyc personal | 46.68% | 47.67% | 67.94% | 51.80% |
| zyf personal | 45.79% | 56.45% | 41.35% | 52.14% |
| author-common6 | 37.56% | 50.00% | 33.17% | 50.00% |
| our-common6 | 44.46% | 48.29% | 36.03% | 46.78% |
| mixed-common6 | 47.48% | 52.68% | 51.03% | 55.77% |

相对 `our-common6`，`mixed-common6` 在 lyc 上 Accuracy / Balanced Accuracy 分别增加 3.01 / 4.39 个百分点，在 zyf 上分别增加 15.00 / 8.99 个百分点。就当前实际数据表示而言，加入 author historical recordings 与 LOCKED_TEST 泛化相关；不能单独解释为 author 数据“有效”，也不能把差异归因于单一 subject、device 或 task/paradigm。

逐窗口预测、predicted class ratio、逐 session 结果和混淆矩阵见 [`artifacts/cross_source_comparison/2026-09-14/`](../../artifacts/cross_source_comparison/2026-09-14/)。common6 三个模型的 LOCKED_TEST `fit_calls=0`；`zqd`、unknown identity、locked reference 均未进入训练。

## 9. 实验室前阶段快照（截至出发前）

本节是 2026-09-14 截至出发实验室前的 EEG 状态快照。它记录今天已经完成的工程和实验准备；**现场录制尚未完成，本节不包含今天新的现场信号、QuickTest 结果或前后改善结论**。

### 9.1 今天已完成

#### Common6 兼容性与比较实验

- Common6 的命名阻塞已解除。当前 DSI-24 的 `T5-Pz → P7-Pz`、`T6-Pz → P8-Pz` 按 nomenclature equivalence 确认，不是空间位置猜测。
- 正式顺序固定为 `F7,F3,P7,O1,O2,P8`；DSI-24 adapter 为 `F7-Pz→F7`、`F3-Pz→F3`、`T5-Pz→P7`、`O1-Pz→O1`、`O2-Pz→O2`、`T6-Pz→P8`；`AF4` 主动舍弃。
- `author-common6`、`our-common6`、`mixed-common6` 已沿用共享 EDF/MAT 读取、预处理、Welch 特征、Scaler、PCA 和 SVC 流程。没有复制第二套算法，也没有修改旧 7 通道 author-only 模型。
- 三模型的既有 CV、LOCKED_TEST、predicted class ratio、session-level 结果和主要比较已登记在本节前文及 [`artifacts/cross_source_comparison/2026-09-14/`](../../artifacts/cross_source_comparison/2026-09-14/)。
- Our EDF reference 为 `Pz (confirmed)`；author MAT reference 仍为 `unknown`。因此跨来源结果只能写作 `exploratory / channel-aligned but reference compatibility uncertain`，不能把差异单独归因于 subject、device 或 task/paradigm。

#### 仓库文档整理

- 根 [`README.md`](../../README.md) 已定位为项目当前概览和新人入口：解释问题、实验路线、模型和路径，但不承担每日 EEG 进展的主事实记录。
- 新增 [`docs/EXPERIMENT_MAP.md`](../EXPERIMENT_MAP.md)、[`docs/MODEL_CATALOG.md`](../MODEL_CATALOG.md) 和 [`docs/REPOSITORY_FILE_GUIDE.md`](../REPOSITORY_FILE_GUIDE.md) 的新人阅读/查询路径。
- 仓库原有 README 已统一补充“目录是什么、属于哪一步、为什么存在、输入来源、生成者、文件说明、状态、使用场景和不要误解”等内容；新增 exploratory/LAB_FEEDBACK 目录 README。
- 本日进展的权威事实源是本文件 `docs/progress/EEG项目推进简报_2026-09-14.md`。后续日终有实质 EEG 进展时，优先更新对应日期简报；不把当天流水账重新塞回根 README。

#### QuickTest 三模型扩展

- 现有 [`notebooks/lab_quick_test_legacy_model.ipynb`](../../notebooks/lab_quick_test_legacy_model.ipynb) 已扩展为现场使用的三个代表模型：Existing pooled frozen、匹配的 `lyc/zyf personal`、`mixed-common6`。
- 推理逻辑集中在 [`scripts/subject_model_utils.py`](../../scripts/subject_model_utils.py)，Notebook 只负责填写路径、调用和显示；没有复制预处理、滤波或特征代码。
- pooled/personal 固定使用完整通道的 240 维特征；mixed-common6 单独使用 Common6 的 60 维特征，禁止把 240 维矩阵直接送入 60 维模型。两路窗口时间轴已做一致性检查。
- 单 EDF 模式显示文件名、subject、预期标签、时长、窗口数、Accuracy、Focus/Unfocus 预测比例和模型对比；Before/After 模式只在同一已知受试者、同一二分类标签且输入不是同一文件时计算改善 Δ。
- `zqd` 或无法可靠识别身份时，明确跳过 personal model，不根据内容猜测身份；仍可运行 pooled 与 mixed-common6。未知标签不计算 Accuracy。
- QuickTest 严格 inference-only：只做 load、transform、predict、compare；不做 `fit`、`fit_transform`、微调、calibration、threshold tuning、model selection 或模型覆盖。新增 [`scripts/validate_quick_test.py`](../../scripts/validate_quick_test.py) 已用真实已有 EDF 验证，拦截到的 fit 调用为 0。

#### LAB_FEEDBACK 协议

- 已建立 [`data/exploratory/lab_feedback/2026-09-14/`](../../data/exploratory/lab_feedback/2026-09-14/) 目录及 README；当前只建立规则，**没有伪造或提前登记 EDF、CSV、DSI、metadata.csv**。
- 文件名约定为 `subject_status_timestamp_extra.edf`，例如 `lyc_focus_202609141630_feedback0.edf`、`lyc_focus_202609141650_feedback1.edf`；同一 recording 的 `.edf`、`.csv`、`.dsi` 保持相同 stem，原始信号不就地修改。
- metadata 至少登记：`subject_id`、`intended_label`、`timestamp`（Asia/Shanghai）、`feedback_round`、`feedback_seen_before_recording`、`task`、`notes`、`dataset_role=lab_feedback`、`eligible_for_training=false`、`eligible_for_final_test=false`；同时保留 `pair_id`、原始文件名、路径和 SHA-256 provenance。
- `feedback0` 通常表示录制前未看本轮模型反馈，`feedback1+` 表示已看反馈并主动调整；若实际过程不同，按事实登记，不只根据后缀反推。
- 包括 `feedback0` 在内，LAB_FEEDBACK 全部属于 exploratory；默认不进入 `legacy_manifest.csv`、LOCKED_TEST 或 future final holdout。即使某次预测结果漂亮，也不能直接晋升为训练数据或最终测试数据。

### 9.2 截至出发前尚未完成（历史快照）

- 今天实验室现场的真实 EDF/CSV/DSI 录制尚未完成，因此没有今天的现场单 EDF 结果、Before/After 结果、预测比例变化或 session-level 现场结论。
- LAB_FEEDBACK 的真实 metadata 尚未填写，SHA-256 尚未登记；当前日期目录只有规则文档。
- 没有基于今天反馈数据训练、微调、calibration、阈值调整或更新任何模型；没有建立新的 final holdout。
- `author MAT reference=unknown` 仍是跨来源解释边界。Common6 的通道命名阻塞已解除，但 reference compatibility uncertainty 没有解除。

### 9.3 今晚回到项目后待补（历史待办；当前结果见第10节）

1. 将实际带回的 EDF/CSV/DSI 原样放入 `data/exploratory/lab_feedback/2026-09-14/`，确认每组文件 stem、文件名、采集时间和文件完整性。
2. 由实际记录确认 `subject_id`、`intended_label`、`feedback_round`、`feedback_seen_before_recording`、`task` 和 `notes`，再填写 metadata；身份无法可靠确认时标为 unknown，不猜测。
3. 为原始文件计算并登记 SHA-256，保留原文件，不把 LAB_FEEDBACK 改放到 `data/locked/`，也不加入 legacy training manifest。
4. 对每个可用 EDF 运行 QuickTest 单文件模式；若同一受试者、同一状态有 feedback0/feedback1+，再运行 Before/After 模式。记录 pooled、personal、mixed-common6 的 Accuracy、目标类别比例、预测类别数量、时长和窗口数。
5. 将现场结果另存为 exploratory 分析产物，并在本文件追加“现场录制完成后”小节；明确记录观察顺序、任务、时长和 reference uncertainty，不把观察性 Δ 写成因果改善。
6. 重新运行只读验收和 Git diff 检查，确认 LOCKED_TEST、旧模型、旧报告和 manifest 未被写入；如 planning-hub 需要日终总 Log，由其读取本日简报并结合 Git diff/commit 核验，本项目本轮不生成或修改 planning-hub 总 Log。

### 9.4 EEG → Planning 状态交接包（出发前历史快照）

**当前状态：`EEG / pre-lab snapshot / ready for field recording`。**

**已完成：** Common6 命名兼容性门禁、三模型 common6 比较、仓库新人文档、QuickTest 三模型 inference-only 扩展、LAB_FEEDBACK 目录和数据协议已完成；旧模型、LOCKED_TEST 和历史正式结果保持不变。

**未完成：** 现场真实录制、现场 QuickTest 输出、Before/After 观察结果、真实 metadata/SHA-256 登记和 exploratory 结果归档均尚未完成。

**Planning 侧应读取的权威来源：** 本文件 `docs/progress/EEG项目推进简报_2026-09-14.md`；根 README 仅用于当前项目概览和新人导航。若生成日终总 Log，应以本简报为主，并用当日 Git diff/commit 核验哪些内容确实发生。

**今晚回传后状态转移：** `pre-lab snapshot` → `recording received` → `metadata/hash verified` → `QuickTest complete` → `exploratory results archived`。在这些条件完成前，不应把今天的 LAB_FEEDBACK 描述为新的最终测试集或训练数据，也不应写成模型已经改善。

## 10. 现场录制完成与数据归档

### 10.1 归档事实

- 2026-09-14 LAB_FEEDBACK 现场录制已完成。本批实际归档 11 条 EDF，均有配套 CSV 和 DSI；文件清单和逐条登记见 [data/exploratory/lab_feedback/2026-09-14/README.md](../../data/exploratory/lab_feedback/2026-09-14/README.md) 与 [metadata.csv](../../data/exploratory/lab_feedback/2026-09-14/metadata.csv)。
- 标签事实来源按 `.md notes > 现场 metadata/明确人工记录 > 文件名`；`model_prediction_used_for_label=false`。两条 `zyf_unfocus` 的现场 `.md` 写明 `condition: focus`，与文件名冲突，因此 `canonical_label=focus`，`filename_label=unfocus` 仅作 provenance；冲突字段和原文均保留在 metadata。
- 实际文件名没有协议要求的 `feedbackX` token，现场 notes 也没有提供反馈轮次。因此 `feedback_round` 与 `feedback_seen_before_recording` 暂登记为 `unknown`，没有按时间顺序猜测。协议映射仍保持：核实为 `feedback0` 时为 `false`，核实为 `feedback1+` 时为 `true`。
- 现场 `.md` 的主观状态、任务、模式和异常说明已合并；未提供的配对关系和佩戴异常字段登记为 `unknown`，不做推断。

### 10.2 原文件完整性与数据边界

- 11/11 EDF 的标准头可读，均为 26 通道、300 Hz；头部记录数对应的理论文件大小与实际文件大小一致。11/11 CSV 和 11/11 DSI 均存在，文件大小和 SHA-256 已登记。CSV 31 列、300 Hz 头信息和时间轴均已保留；CSV 与 EDF duration 的差异没有被静默修正。
- 所有本批记录在后续结构整理中统一归档为 `dataset_role=historical_pilot`、`experiment_version=pre_new_paradigm/lab_feedback_2026-09-14`，且 training/validation/final-test eligibility 均为 `false`。本批没有写入 `data/legacy_manifest.csv`、`data/session_manifest.csv`、`data/locked/` 或 New Paradigm v1 manifest。
- 本次只做扫描、哈希、标准头/配套文件完整性检查、metadata/标签登记和文档更新。没有训练、调参、微调、calibration、阈值调整或正式评估，也没有把现场观察写成模型最终结论。

## 11. LAB_FEEDBACK 标签纠正与 prediction-only 分析

### 11.1 标签事实

- 已按 `.md notes > 现场 metadata/明确人工记录 > filename_label` 重新登记标签。`zyf_unfocus_202609141641.edf` 与 `zyf_unfocus_202609141707.edf` 的 filename label 为 `unfocus`，但同 stem `.md` 的 `condition` 为 `focus`，最终 `canonical_label=focus`；原始文件名、`.md` 和信号内容均未修改。
- 11 条记录的 `feedback_round` 与 `feedback_seen_before_recording` 继续为 `unknown`。真实 timestamp 只用于 chronological session sequence 排序，不推断 feedback round、pair 或反馈前后关系。

### 11.2 三模型输出

- 11 条 EDF 均运行 Existing pooled frozen、对应 subject personal、mixed-common6 的统一 prediction-only 入口；pooled/personal 使用 240 维，mixed-common6 使用独立 60 维 common6 特征。结果和逐模型计数见 [LAB_FEEDBACK 报告](../../artifacts/lab_feedback/2026-09-14/REPORT.md) 与 [session_model_summary.csv](../../artifacts/lab_feedback/2026-09-14/session_model_summary.csv)。
- 结果仅按 canonical label 计算 window accuracy 和 target-class proportion。报告另外登记同一 subject/label 的 session 波动、三模型一致性、personal 独自异常和 zyf 17:52/18:10 对照；这些都是 exploratory 描述，不是 calibration、attention state 因果或最终模型结论。
- 本批观察到 session 间输出存在差异；由于只有单日数据且 feedback round 未知，不能据此确认 day shift 或 feedback-driven improvement。mixed-common6 是否更稳定也只按报告中的小样本描述性离散度呈现，不作模型优越性结论。

### 11.3 数据边界与运行保护

- 全部 11 条继续为 `dataset_role=historical_pilot`、`experiment_version=pre_new_paradigm/lab_feedback_2026-09-14`，training/validation/final-test eligibility 均为 `false`，没有写入 `legacy_manifest.csv`、`LOCKED_TEST`、New Paradigm v1 manifest 或 future final holdout。
- 批处理脚本在运行期间拦截 `fit`、`fit_transform`、`partial_fit` 和模型写出；`run_manifest.json` 登记 `prediction_only=true`、`training_performed=false`、`fit_calls=0`。运行时仅有保存模型 1.9.1 与当前 scikit-learn 1.9.0 的兼容性警告，未安装依赖、未保存或重训模型。

## 12. New Paradigm v1 转向登记（2026-09-15 补记）

- 2026-09-14 的 11 条 LAB_FEEDBACK 已正式冻结为 historical pilot / transition dataset。原始 EDF/CSV/DSI/notes、metadata 和 prediction-only 结果保持原路径，未写入旧 `data/session_manifest.csv`。
- 当前研究入口改为 `data/current/new_paradigm_v1/` 与 `docs/current/`。新 manifest 目前只有 schema、0 条 session；没有将历史数据伪装成新范式数据。
- 七个旧模型统一标记为 `historical baseline`。它们用于复现、对照和解释研究路线，不再作为 New Paradigm v1 默认模型。
- 未来首先只用 `new_paradigm_v1` 合格 session 建立 `lyc-new personal`、`zyf-new personal` 和 `new-paradigm pooled`；本次结构整理没有训练、微调、calibration、阈值或超参数调整。
